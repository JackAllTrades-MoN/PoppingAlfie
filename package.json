{
  "name": "cpop",
  "version": "1.0.0",
  "description": "```bash dune build @deploy ```",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "dune build; webpack-dev-server",
    "build": "dune build; webpack build",
    "deploy": "dune build; webpack build && ./deploy.sh"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "webpack": "^5.88.2",
    "webpack-cli": "^5.1.4",
    "webpack-dev-server": "^4.15.1"
  }
}
